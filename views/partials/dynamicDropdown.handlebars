<!-- Select Basic -->
<div class="form-group">
    <select name="{{key}}">
    <option value="" disabled selected>{{prettyKey}}</option>
    {{#each entries}}
        <option value="{{this.id}}">{{this.name}}</option>
    {{/each}}
    </select>
</div>